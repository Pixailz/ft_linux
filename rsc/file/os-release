NAME="${LFS_DISTRO_DESC}"
VERSION="${LFS_DISTRO_VERSION}"
ID="${LFS_DISTRO_MINI}"
PRETTY_NAME="${LFS_DISTRO_DESC} ${LFS_DISTRO_VERSION}"
VERSION_CODENAME="${LFS_DISTRO_CODENAME}"
HOME_URL="${LFS_DISTRO_HOME}"
